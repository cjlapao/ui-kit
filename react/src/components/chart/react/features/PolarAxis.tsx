/**
 * <Chart.PolarAxis> — configuration for the shared polar grid (ring
 * shape/lines, grid style, tick labels, category sort). The root reads its
 * props through the children summary and paints the grid itself, so this
 * element renders nothing.
 */
import { PolarAxisProps } from "../props";

export function PolarAxis(_props: PolarAxisProps = {}) {
  return null;
}
