/**
 * <Chart.RadarAxis> — configuration for the shared radar grid (ring count,
 * outer value, tick label format). The root reads its props through the
 * children summary and paints the grid itself, so this element renders
 * nothing.
 */
import { RadarAxisProps } from "../props";

export function RadarAxis(_props: RadarAxisProps = {}) {
  return null;
}
